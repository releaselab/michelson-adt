parameter nat;
storage nat;
code {  CAR; PUSH @index nat 1; DUP @acc;
        PUSH bool True;
        LOOP { DIP { DUP @i;
                     DIP { PUSH nat 1; ADD @ipp } };
               MUL @tempRes;
               DIP { DIP { DUP @n };
                     DUP @ipp2;
                     DIP { SWAP }; 
                     COMPARE; LE };
               SWAP };
        DIP { DROP; DROP };
        NIL operation; PAIR };
