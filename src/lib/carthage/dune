(library
 (name carthage)
 (public_name michelson.carthage)
 (libraries loc tezos-micheline core_kernel bignum)
 (preprocess
  (pps ppx_deriving.ord ppx_sexp_conv)))

(ocamllex
 (modules lexer))

(menhir
 (modules parser))
