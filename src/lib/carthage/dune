(library
 (name carthage)
 (public_name michelson.carthage)
 (libraries loc core_kernel bignum hex)
 (preprocess
  (pps ppx_deriving.ord ppx_sexp_conv)))

(ocamllex
 (modules lexer))

(menhir
 (modules parser)
 (flags --explain))
