(library
 (name micheline)
 (public_name michelson.micheline)
 (libraries base bignum hex)
 (preprocess
  (pps ppx_deriving.ord ppx_deriving.eq ppx_sexp_conv)))

(ocamllex
 (modules lexer))

(menhir
 (modules parser)
 (flags --explain))
